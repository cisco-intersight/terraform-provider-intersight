// Code generated by go-swagger; DO NOT EDIT.

package models

// This file was generated by the swagger tool.
// Editing this file might prove futile when you re-run the swagger generate command

import (
	"encoding/json"

	strfmt "github.com/go-openapi/strfmt"

	"github.com/go-openapi/errors"
	"github.com/go-openapi/swag"
)

// PolicyConfigContext Policy:Config Context
//
// Configuration related state and results.
//
// swagger:model policyConfigContext
type PolicyConfigContext struct {
	PolicyConfigContextAO0P0
}

// UnmarshalJSON unmarshals this object from a JSON structure
func (m *PolicyConfigContext) UnmarshalJSON(raw []byte) error {
	// AO0
	var aO0 PolicyConfigContextAO0P0
	if err := swag.ReadJSON(raw, &aO0); err != nil {
		return err
	}
	m.PolicyConfigContextAO0P0 = aO0

	return nil
}

// MarshalJSON marshals this object to a JSON structure
func (m PolicyConfigContext) MarshalJSON() ([]byte, error) {
	_parts := make([][]byte, 0, 1)

	aO0, err := swag.WriteJSON(m.PolicyConfigContextAO0P0)
	if err != nil {
		return nil, err
	}
	_parts = append(_parts, aO0)

	return swag.ConcatJSON(_parts...), nil
}

// Validate validates this policy config context
func (m *PolicyConfigContext) Validate(formats strfmt.Registry) error {
	var res []error

	// validation for a type composition with PolicyConfigContextAO0P0
	if err := m.PolicyConfigContextAO0P0.Validate(formats); err != nil {
		res = append(res, err)
	}

	if len(res) > 0 {
		return errors.CompositeValidationError(res...)
	}
	return nil
}

// MarshalBinary interface implementation
func (m *PolicyConfigContext) MarshalBinary() ([]byte, error) {
	if m == nil {
		return nil, nil
	}
	return swag.WriteJSON(m)
}

// UnmarshalBinary interface implementation
func (m *PolicyConfigContext) UnmarshalBinary(b []byte) error {
	var res PolicyConfigContext
	if err := swag.ReadJSON(b, &res); err != nil {
		return err
	}
	*m = res
	return nil
}

// PolicyConfigContextAO0P0 policy config context a o0 p0
// swagger:model PolicyConfigContextAO0P0
type PolicyConfigContextAO0P0 struct {

	// Indicates a profile's configuration deploying state. Values -- Assigned, Not-assigned, Associated, Pending-changes, Validating, Configuring, Failed.
	//
	// Read Only: true
	ConfigState string `json:"ConfigState,omitempty"`

	// System action to trigger the appropriate workflow. Values -- No_op, ConfigChange, Deploy, Unbind.
	//
	ControlAction string `json:"ControlAction,omitempty"`

	// Indicates a profile's error state. Values -- Validation-error (Static validation error), Pre-config-error (Runtime validation error), Config-error (Runtime configuration error).
	//
	ErrorState string `json:"ErrorState,omitempty"`

	// The concrete type of this complex type.
	//
	// The ObjectType property must be set explicitly by API clients when the type is ambiguous. In all other cases, the
	// ObjectType is optional.
	// The type is ambiguous when a managed object contains an array of nested documents, and the documents in the array
	// are heterogeneous, i.e. the array can contain nested documents of different types.
	//
	//
	ObjectType string `json:"ObjectType,omitempty"`

	// Combined state (configState, and operational state of the associated physical resource) to indicate the current state of the profile. Values -- n/a, Power-off, Pending-changes, Configuring, Ok, Failed.
	//
	// Read Only: true
	OperState string `json:"OperState,omitempty"`

	// policy config context a o0 p0
	PolicyConfigContextAO0P0 map[string]interface{} `json:"-"`
}

// UnmarshalJSON unmarshals this object with additional properties from JSON
func (m *PolicyConfigContextAO0P0) UnmarshalJSON(data []byte) error {
	// stage 1, bind the properties
	var stage1 struct {

		// Indicates a profile's configuration deploying state. Values -- Assigned, Not-assigned, Associated, Pending-changes, Validating, Configuring, Failed.
		//
		// Read Only: true
		ConfigState string `json:"ConfigState,omitempty"`

		// System action to trigger the appropriate workflow. Values -- No_op, ConfigChange, Deploy, Unbind.
		//
		ControlAction string `json:"ControlAction,omitempty"`

		// Indicates a profile's error state. Values -- Validation-error (Static validation error), Pre-config-error (Runtime validation error), Config-error (Runtime configuration error).
		//
		ErrorState string `json:"ErrorState,omitempty"`

		// The concrete type of this complex type.
		//
		// The ObjectType property must be set explicitly by API clients when the type is ambiguous. In all other cases, the
		// ObjectType is optional.
		// The type is ambiguous when a managed object contains an array of nested documents, and the documents in the array
		// are heterogeneous, i.e. the array can contain nested documents of different types.
		//
		//
		ObjectType string `json:"ObjectType,omitempty"`

		// Combined state (configState, and operational state of the associated physical resource) to indicate the current state of the profile. Values -- n/a, Power-off, Pending-changes, Configuring, Ok, Failed.
		//
		// Read Only: true
		OperState string `json:"OperState,omitempty"`
	}
	if err := json.Unmarshal(data, &stage1); err != nil {
		return err
	}
	var rcv PolicyConfigContextAO0P0

	rcv.ConfigState = stage1.ConfigState

	rcv.ControlAction = stage1.ControlAction

	rcv.ErrorState = stage1.ErrorState

	rcv.ObjectType = stage1.ObjectType

	rcv.OperState = stage1.OperState

	*m = rcv

	// stage 2, remove properties and add to map
	stage2 := make(map[string]json.RawMessage)
	if err := json.Unmarshal(data, &stage2); err != nil {
		return err
	}

	delete(stage2, "ConfigState")

	delete(stage2, "ControlAction")

	delete(stage2, "ErrorState")

	delete(stage2, "ObjectType")

	delete(stage2, "OperState")

	// stage 3, add additional properties values
	if len(stage2) > 0 {
		result := make(map[string]interface{})
		for k, v := range stage2 {
			var toadd interface{}
			if err := json.Unmarshal(v, &toadd); err != nil {
				return err
			}
			result[k] = toadd
		}
		m.PolicyConfigContextAO0P0 = result
	}

	return nil
}

// MarshalJSON marshals this object with additional properties into a JSON object
func (m PolicyConfigContextAO0P0) MarshalJSON() ([]byte, error) {
	var stage1 struct {

		// Indicates a profile's configuration deploying state. Values -- Assigned, Not-assigned, Associated, Pending-changes, Validating, Configuring, Failed.
		//
		// Read Only: true
		ConfigState string `json:"ConfigState,omitempty"`

		// System action to trigger the appropriate workflow. Values -- No_op, ConfigChange, Deploy, Unbind.
		//
		ControlAction string `json:"ControlAction,omitempty"`

		// Indicates a profile's error state. Values -- Validation-error (Static validation error), Pre-config-error (Runtime validation error), Config-error (Runtime configuration error).
		//
		ErrorState string `json:"ErrorState,omitempty"`

		// The concrete type of this complex type.
		//
		// The ObjectType property must be set explicitly by API clients when the type is ambiguous. In all other cases, the
		// ObjectType is optional.
		// The type is ambiguous when a managed object contains an array of nested documents, and the documents in the array
		// are heterogeneous, i.e. the array can contain nested documents of different types.
		//
		//
		ObjectType string `json:"ObjectType,omitempty"`

		// Combined state (configState, and operational state of the associated physical resource) to indicate the current state of the profile. Values -- n/a, Power-off, Pending-changes, Configuring, Ok, Failed.
		//
		// Read Only: true
		OperState string `json:"OperState,omitempty"`
	}

	stage1.ConfigState = m.ConfigState

	stage1.ControlAction = m.ControlAction

	stage1.ErrorState = m.ErrorState

	stage1.ObjectType = m.ObjectType

	stage1.OperState = m.OperState

	// make JSON object for known properties
	props, err := json.Marshal(stage1)
	if err != nil {
		return nil, err
	}

	if len(m.PolicyConfigContextAO0P0) == 0 {
		return props, nil
	}

	// make JSON object for the additional properties
	additional, err := json.Marshal(m.PolicyConfigContextAO0P0)
	if err != nil {
		return nil, err
	}

	if len(props) < 3 {
		return additional, nil
	}

	// concatenate the 2 objects
	props[len(props)-1] = ','
	return append(props, additional[1:]...), nil
}

// Validate validates this policy config context a o0 p0
func (m *PolicyConfigContextAO0P0) Validate(formats strfmt.Registry) error {
	return nil
}

// MarshalBinary interface implementation
func (m *PolicyConfigContextAO0P0) MarshalBinary() ([]byte, error) {
	if m == nil {
		return nil, nil
	}
	return swag.WriteJSON(m)
}

// UnmarshalBinary interface implementation
func (m *PolicyConfigContextAO0P0) UnmarshalBinary(b []byte) error {
	var res PolicyConfigContextAO0P0
	if err := swag.ReadJSON(b, &res); err != nil {
		return err
	}
	*m = res
	return nil
}
